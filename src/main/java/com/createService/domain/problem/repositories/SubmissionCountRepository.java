package com.createService.domain.problem.repositories;

import com.createService.domain.problem.model.SubmissionCount;

public interface SubmissionCountRepository {

    void create(SubmissionCount submissionCount);
}
